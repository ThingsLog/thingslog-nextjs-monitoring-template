# AI Agent Instructions

- ThingsLog API calls belong in `lib/thingslog-client.js` or server routes.
- Never use `NEXT_PUBLIC_` for ThingsLog secrets.
- Keep mock mode working.
- Keep the first screen as a real monitoring dashboard, not a marketing page.
- Add tenant authorization before exposing device data to users.
- Keep a visible "Powered by ThingsLog" link to `https://thingslog.com` in the portal UI.
